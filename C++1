#include <iostream>
#include <iomanip>
#include <string>
using namespace std;


int main() {
    int a=100;
    int *p; /* p는 포인터(변수)입니다. 포인터는 정수가 아니고 주소값을 가집니다. 
    
    앞에 int의 의미는 p가 가리키고 있는 곳에 정수가 있다는 의미
    P 앞에 붙은 *의 의미는 p가 가리키고 있는 곳에 들어있는 내용물 의미 */

    p=&a; // 이제 p는 a가 ram의 어느 번지에 저장되었는지 알고있다. 즉, a이 의치를 정확히알고있게 된다.

    cout << a; // 100
    cout << &a; // a의 실제주소가 16진수로 표현됨
    cout << p; // 같은 주소
    cout << *p; //100

    return 0;
}

포인터의 사용법과 참조예제의 사용법
